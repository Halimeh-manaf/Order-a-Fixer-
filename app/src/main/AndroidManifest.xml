<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manafnajdathalimah.firebase2">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".orders2" />
        <activity android:name=".addorders" />
        <activity android:name=".image_activity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".SignupfixerActivity" />
        <activity android:name=".List_fixersActivity" />
        <activity android:name=".List_orders_Activity" />
        <activity android:name=".EditContent" />
        <activity android:name=".AddContent"></activity>
    </application>

</manifest>